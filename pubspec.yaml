name: timmy_web_flutter
description: Developer – Antônio

publish_to: "none"

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  assets:
    - assets/
  fonts:
    - family: Quicksand
      fonts:
        - asset: assets/Quicksand-Regular.ttf
  uses-material-design: true
